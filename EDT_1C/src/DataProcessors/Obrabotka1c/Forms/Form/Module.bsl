
&НаСервере
Процедура РинатНаСервере()
	
	мСтр = "Лев,Дева,Весы,Итд";
	мРез = ph_РасширенныеВозможности.ПреобразоватьСтрокуВМассив(мСтр);
	Для Каждого Элемент Из мРез Цикл
		Сообщить(Элемент);
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура Ринат(Команда)
	РинатНаСервере();
КонецПроцедуры
